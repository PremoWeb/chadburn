version: "3"

services:
  chronos:
    build:
      dockerfile: Dockerfile
      context: ../../.
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./chronos.ini:/etc/chronos/config.ini

  job:
    image: hello-world