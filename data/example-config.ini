[global]
# Global settings
save-folder = /var/log/chadburn

# Example job that runs every hour
[job-local "example-job"]
schedule = @hourly
command = echo "Hello, world!"

# Example job that runs every 5 minutes
[job-local "frequent-job"]
schedule = */5 * * * *
command = date > /tmp/current-time.txt 